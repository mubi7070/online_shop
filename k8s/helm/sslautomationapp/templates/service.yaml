apiVersion: v1
kind: Service
metadata:
  name: {{ include "sslautomationapp.fullname" . }}
  labels:
    {{- include "sslautomationapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "sslautomationapp.selectorLabels" . | nindent 4 }}
