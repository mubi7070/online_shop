apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sslautomationapp.fullname" . }}-test-connection"
  labels:
    {{- include "sslautomationapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sslautomationapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
